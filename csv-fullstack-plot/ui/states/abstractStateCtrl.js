angular.module('myApp')
.controller('abstractStateCtrl', ['$scope', '$state', function($scope, $state) {
    // expose global configuration to global scope, so they can be referred to outside of states if needed
}])
